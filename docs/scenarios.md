# Scenarios
The application contains three scenarios:

1. Tender → Risk → Bid Decision
2. Customer Input → I/O → Cross-check → FAT
3. Vendor Quote → Compliance → Recommendation

Use synthetic data.

Do not add authentication, databases, RAG, OCR, document ingestion, enterprise integrations, or other production infrastructure unless explicitly requested.

